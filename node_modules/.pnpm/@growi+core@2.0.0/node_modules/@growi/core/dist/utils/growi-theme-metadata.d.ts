import { ColorScheme } from '../interfaces/color-scheme';
import { GrowiThemeSchemeType } from '../interfaces/growi-theme-metadata';

export declare const getForcedColorScheme: (growiThemeSchemeType?: GrowiThemeSchemeType) => ColorScheme | undefined;
