import { scanAllTemplates as o } from "./scan.js";
import { validateTemplatePluginGrowiDirective as n } from "./validate-growi-plugin-directive.js";
const d = async (l) => {
  const r = n(l), s = await o(l, {
    data: r,
    returnsInvalidTemplates: !0
  });
  if (Object.keys(s).length === 0)
    throw new Error("This plugin does not have any templates");
  const i = {};
  for (const a of s)
    i[a.default.id] = Object.values(a).map(
      (e) => (e == null ? void 0 : e.isValid) ?? !1
    );
  for (const [a, e] of Object.entries(i))
    if (e.every((t) => t) || console.warn(
      `[WARN] Template '${a}' has some locales that status is invalid`
    ), !e.some((t) => t))
      return !1;
  return !0;
};
export {
  d as validateAllTemplateLocales
};
//# sourceMappingURL=validate-all-locales.js.map
