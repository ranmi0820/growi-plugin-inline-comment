import { GrowiPluginType as r } from "@growi/core";
import { GrowiPluginValidationError as l } from "../../../../model/growi-plugin-validation-error.js";
import { validateGrowiDirective as a } from "../common/validate-growi-plugin-directive.js";
const s = (e) => {
  const t = a(e, r.Template), { growiPlugin: i } = t, o = i.locales;
  if (o == null || o.length === 0)
    throw new l(
      "Template plugin must have 'supportingLocales' and that must have one or more locales",
      {
        ...t,
        supportingLocales: o
      }
    );
  return {
    ...t,
    supportingLocales: o
  };
};
export {
  s as validateTemplatePluginGrowiDirective
};
//# sourceMappingURL=validate-growi-plugin-directive.js.map
