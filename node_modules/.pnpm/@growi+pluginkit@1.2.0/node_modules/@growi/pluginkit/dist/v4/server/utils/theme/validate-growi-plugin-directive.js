import { GrowiPluginType as n, isGrowiThemeMetadata as h } from "@growi/core";
import { GrowiPluginValidationError as o } from "../../../../model/growi-plugin-validation-error.js";
import { validateGrowiDirective as s } from "../common/validate-growi-plugin-directive.js";
const w = (m) => {
  const r = s(m, n.Theme), { growiPlugin: e } = r;
  if (e.themes == null || !Array.isArray(e.themes) || e.themes.length === 0)
    throw new o(
      "Theme plugin must have 'themes' array and that must have one or more theme metadata"
    );
  const a = [], t = [];
  for (const i of e.themes)
    h(i) ? a.push(i) : t.push(i);
  if (t.length > 0)
    throw new o(
      `Some of theme metadata are invalid: ${t.toString()}`
    );
  return {
    ...r,
    themes: a
  };
};
export {
  w as validateThemePluginGrowiDirective
};
//# sourceMappingURL=validate-growi-plugin-directive.js.map
